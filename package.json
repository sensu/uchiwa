{
  "name": "uchiwa",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "gotest": "go test ./... -cover -test.v",
    "postinstall": "bower --allow-root install"
  },
  "dependencies": {
    "bower": "^1.3.4"
  }
}
