{
  "name": "uchiwa",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "gotest": "go test ./... -cover -test.v",
    "postinstall": "bower --allow-root install"
  },
  "dependencies": {
    "bower": "^1.8.0"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-cache-breaker": "^2.0.1",
    "grunt-cli": "^0.1.13"
  }
}
